pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "MarvelMultiplataforma"
include(":androidApp")
include(":shared")